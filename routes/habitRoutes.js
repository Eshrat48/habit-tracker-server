// routes/habitRoutes.js

const express = require('express');
const router = express.Router();

// This router instance will be configured in index.js after the DB connection is established.
module.exports = router;